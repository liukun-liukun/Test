server:
  port: 18082

spring:
  application:
    name: test
  profiles:
    active: dev
  mvc:
    format:
      date: yyyy-MM-dd HH:mm:ss
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
logging:
  file:
    name: C:/source/NTSPlat/logs/${spring.application.name}-${server.port}-info.log